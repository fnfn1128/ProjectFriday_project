<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>🎉 기념일 상세보기</title>
        <link href="https://fonts.googleapis.com/css2?family=Gmarket+Sans:wght@400;700&display=swap" rel="stylesheet">
        <style>
            body {
                font-family: 'Gmarket Sans', 'Noto Sans KR', Arial, sans-serif;
                background: linear-gradient(135deg, #FFF5E1 0%, #FFD6EC 100%);
                margin: 0;
                min-height: 100vh;
            }
            .header-bar {
                display: flex;
                justify-content: flex-end;
                align-items: center;
                max-width: 480px;
                margin: 0 auto;
                padding: 24px 28px 0 28px;
            }
            .btn.list-link {
                width: auto;
                min-width: 120px;
                padding: 10px 26px 10px 20px;
                font-size: 1.02rem;
                border-radius: 9px;
                background: linear-gradient(90deg, #ffd6ec 0%, #ffb347 100%);
                color: #ff5e62;
                font-weight: 700;
                box-shadow: none;
                text-align: right;
                margin-left: auto;
                margin-right: 0;
                text-decoration: none;
                display: inline-block;
                transition: background 0.22s, color 0.13s, transform 0.1s;
            }
            .btn.list-link:hover {
                background: linear-gradient(90deg, #ffb347 0%, #ffd6ec 100%);
                color: #fff;
                transform: scale(1.04);
                text-decoration: underline;
            }
            .party-header {
                text-align: center;
                padding-top: 8px;
                margin-bottom: 8px;
            }
            .party-header .emoji {
                font-size: 42px;
                margin-bottom: 0;
                animation: pop 1s infinite alternate;
            }
            @keyframes pop {
                0% { transform: scale(1);}
                100% { transform: scale(1.12) rotate(-4deg);}
            }
            .party-header h2 {
                font-family: 'Gmarket Sans', cursive;
                color: #ff5e62;
                font-size: 2.1rem;
                margin: 0 0 6px 0;
                letter-spacing: 1px;
                text-shadow: 1px 3px 12px #ffdae6;
            }
            .flags {
                text-align: center;
                margin-bottom: 20px;
            }
            .flag {
                display: inline-block;
                width: 26px;
                height: 26px;
                border-radius: 40% 60% 65% 35% / 60% 40% 60% 40%;
                background: linear-gradient(120deg, #ffb347 0%, #ffcc33 100%);
                margin: 0 3px;
                box-shadow: 0 3px 7px rgba(255,183,197,0.23);
                transform: rotate(-8deg);
            }
            .flag:nth-child(2) {
                background: linear-gradient(120deg, #7ee8fa 0%, #eec0c6 100%);
                transform: rotate(8deg);
            }
            .flag:nth-child(3) {
                background: linear-gradient(120deg, #ff6e7f 0%, #bfe9ff 100%);
                transform: rotate(-5deg);
            }
            .flag:nth-child(4) {
                background: linear-gradient(120deg, #d9afd9 0%, #97d9e1 100%);
                transform: rotate(6deg);
            }
            .container {
                max-width: 400px;
                background: #fff;
                margin: 0 auto 0 auto;
                margin-bottom: 36px;
                padding: 38px 28px 26px 28px;
                border-radius: 22px;
                box-shadow: 0 4px 30px 4px #ffd6ec8e, 0 1.5px 6px 0 #eec0c646;
            }
            .show-group {
                margin-bottom: 18px;
                font-size: 1.14rem;
            }
            .label {
                font-weight: bold;
                color: #ff5e62;
                min-width: 52px;
                display: inline-block;
            }
            .value {
                color: #444;
                margin-left: 7px;
                word-break: break-all;
            }
            .dday {
                margin-top: 6px;
                margin-bottom: 10px;
                font-size: 1.18rem;
                font-weight: bold;
                color: #ff5e62;
                letter-spacing: 0.3px;
            }
            .actions {
                display: flex;
                gap: 10px;
                margin-top: 24px;
            }
            .btn.action {
                width: 100%;
                padding: 13px;
                font-size: 1.02rem;
                border-radius: 9px;
                font-weight: 700;
                font-family: 'Gmarket Sans', Arial, sans-serif;
                box-shadow: 0 1.5px 6px 0 #eec0c646;
                transition: background 0.22s, color 0.13s, transform 0.09s;
                text-align: center;
                text-decoration: none;
                border: none;
                cursor: pointer;
            }
            .btn.edit {
                background: linear-gradient(90deg, #eec0c6 0%, #f9e6b1 100%);
                color: #b266e3;
            }
            .btn.delete {
                background: linear-gradient(90deg, #ffbaba 0%, #ffd6ec 100%);
                color: #ff5e62;
            }
            .btn.action:hover {
                filter: brightness(0.97);
                transform: translateY(-2px) scale(1.04);
            }
        </style>
    </head>
    <body>
    <div class="header-bar">
        <a class="btn list-link" href="/anniversaries">← 목록보기</a>
    </div>
    <div class="party-header">
        <div class="emoji">🎉✨🎈</div>
        <h2>기념일 상세보기</h2>
    </div>
    <div class="flags">
        <div class="flag"></div>
        <div class="flag"></div>
        <div class="flag"></div>
        <div class="flag"></div>
    </div>
    <div class="container">
        <div class="show-group">
            <span class="label">제목</span>
            <span class="value">{{anniversary.title}}</span>
        </div>
        <div class="show-group">
            <span class="label">날짜</span>
            <span class="value">{{anniversary.date}}</span>
        </div>
        <div class="show-group dday">
            <span class="label">D-day</span>
            <span class="value">{{anniversary.dayString}}</span>
        </div>
        <div class="show-group">
            <span class="label">메모</span>
            <span class="value">{{anniversary.memo}}</span>
        </div>
        <div class="actions">
            <a class="btn action edit" href="/anniversaries/{{anniversary.id}}/edit">수정</a>
            <a class="btn action delete" href="/anniversaries/{{anniversary.id}}/delete"
               onclick="return confirm('정말로 삭제하시겠습니까?');">삭제</a>
        </div>
    </div>
    </body>
    </html>
</body>
</html>